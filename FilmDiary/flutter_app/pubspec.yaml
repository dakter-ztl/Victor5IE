name: film_diary
description: FilmDiary – Diario Cinematografico Personale (progetto finale)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Chiamate HTTP al server REST PHP
  http: ^1.2.1

  # Cache locale SQLite offline
  sqflite: ^2.3.3+1
  path: ^1.9.0
  connectivity_plus: 6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
